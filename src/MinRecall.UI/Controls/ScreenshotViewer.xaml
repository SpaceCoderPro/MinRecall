<ContentDialog
    x:Class="MinRecall.UI.Controls.ScreenshotViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinRecall.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Screenshot Viewer"
    PrimaryButtonText="Close"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <Grid MinWidth="800" MinHeight="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Info -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock x:Name="ProcessNameText"
                       Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <TextBlock x:Name="WindowTitleText"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       TextWrapping="Wrap"/>
            <TextBlock x:Name="TimestampText"
                       Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Image Viewer -->
        <ScrollViewer Grid.Row="1"
                      ZoomMode="Enabled"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Image x:Name="ScreenshotImage"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"/>
        </ScrollViewer>

        <!-- Actions -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,16,0,0">
            <Button x:Name="ExportButton"
                    Content="Export"
                    Click="ExportButton_Click">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Export as PNG" Click="ExportPng_Click"/>
                        <MenuFlyoutItem Text="Export as JPEG" Click="ExportJpeg_Click"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <Button x:Name="CopyButton"
                    Content="Copy to Clipboard"
                    Click="CopyButton_Click"/>
        </StackPanel>
    </Grid>
</ContentDialog>
