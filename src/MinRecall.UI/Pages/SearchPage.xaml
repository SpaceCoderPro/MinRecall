<Page
    x:Class="MinRecall.UI.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinRecall.UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Search"
                   Style="{StaticResource HeaderTextBlockStyle}"
                   Margin="24,20,0,12"/>

        <!-- Search Box -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="24,0,0,16">
            <AutoSuggestBox x:Name="SearchBox"
                           Width="400"
                           PlaceholderText="Search screenshots by window title or OCR text..."
                           TextChanged="SearchBox_TextChanged"
                           QuerySubmitted="SearchBox_QuerySubmitted"/>

            <CalendarDatePicker x:Name="StartDatePicker"
                               Header="From"
                               PlaceholderText="Start date"
                               Width="160"
                               Margin="12,0,0,0"/>

            <CalendarDatePicker x:Name="EndDatePicker"
                               Header="To"
                               PlaceholderText="End date"
                               Width="160"
                               Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Search Results -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="SearchResultsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="12,6"
                                Padding="16"
                                Tapped="SearchResult_Tapped">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0" Width="240" Height="135">
                                    <Image Source="{Binding Thumbnail}"
                                           Stretch="UniformToFill"
                                           CornerRadius="4"/>
                                </Grid>

                                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Timestamp}"
                                               Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="{Binding ProcessName}"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding WindowTitle}"
                                               Style="{ThemeResource BodyTextBlockStyle}"
                                               TextWrapping="Wrap"
                                               MaxLines="2"
                                               Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding MatchedText}"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,8,0,0"
                                               FontStyle="Italic"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
